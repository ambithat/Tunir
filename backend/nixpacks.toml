[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql", "openssl", "libmysqlclient", "pkg-config", "libGL", "glib"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "bash start.sh"

